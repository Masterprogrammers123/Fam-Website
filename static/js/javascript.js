console.warn("[RUNNING] App is starting [RUNNING]")
/*
function checkForm() { // Probably to be deleted
    console.log("Validating form...")
    var name = document.getElementById("nameSmall")
    var email = document.getElementById("emailSmall")
    var pass = document.getElementById("passSmall")
    var tosChecked = document.getElementById("checkSmall")
    console.log(name, email, pass, tosChecked)

    if (name.length > 100) {
        console.error("Name is longer then 100")
        document.getElementById("nameSmall").innerHTML = "Your name has to be under 100 characters.";
    } else if (name.length == 100) {
        console.warn("Name is 100")
    } else if (name.length < 100 && name.length > 0) {
        console.log("Name is lower then 100")
    } else if (name.length == 0) {
        console.error("Name is 0 chars long")
        document.getElementById("nameSmall").innerHTML = "Your name cant be blank";
    }

    if (email.length > 100) {
        document.getElementById("emailSmall").innerHTML = "Your email has to be under 100 characters.";
    } else if (email.length == 100) {
        console.warn("Email is 100")
    } else if (email.length < 100 && email.length > 0) {
        console.log("Email is lower then 100")
    } else if (email.length == 0) {
        console.error("Email is 0 chars long")
        document.getElementById("emailSmall").innerHTML = "Your email cant be blank";
    }

    if (pass.length > 100) {
        console.error("Password is too long")
        document.getElementById("passSmall").innerHTML = "Your password has to be under 100 characters.";
    } else if (pass.length == 100) {
        console.warn("Password is 100")
    } else if (pass.length < 100 && pass.length > 0) {
        console.log("Password is under 100")
    } else if (pass.length == 0) {
        console.error("Password is 0 chars long")
        document.getElementById("passSmall").innerHTML = "Your password cant be blank";
    }
    // Dont forget to check if they clicked
}


$(function() {
    $('#submit-button-for-signup').bind('click', function() {
        $.getJSON('/signup',
            function(data) {
            //do nothing
        });
    return false;
    });
}); */
